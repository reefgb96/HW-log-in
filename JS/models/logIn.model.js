'use strict';

class User {
    firstName;
    lastName;
    email;
    pass;
    constructor(firstName, lastName, email, pass){
        this.firstName = firstName;
        this.lastName = lastName;
        this.email = email;
        this.pass = pass;
    }
}